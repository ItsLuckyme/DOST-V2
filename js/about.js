// Ensure the animation runs only when the page has fully loaded
window.addEventListener('load', function() {
    const aboutText = document.getElementById('about-text');
    aboutText.style.animation = 'slideIn 2s forwards';
  });
  